@model IEnumerable<StudentsNotebook.Models.Kurs>

@{
	ViewData["Title"] = "Courses";
}

<h1>Lista Kursów</h1>

<table class="table">
	<thead>
		<tr>
			<th>Id kursu: </th>
			<th>Nazwa kursu: </th>
			<th>Prowadzący: </th>
			<th>Trudność kursu: </th>
		</tr>
	</thead>
	<tbody>
		@foreach (var kursek in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => kursek.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => kursek.Coursename)
				</td>
				<td>
					@Html.DisplayFor(modelItem => kursek.Professor)
				</td>
				<td>
					@Html.DisplayFor(modelItem => kursek.Learndifficulty)
				</td>
				<td>
					@Html.ActionLink("Materiały", "Materials", new { id = kursek.Id})
				</td>
			</tr>
		}
	</tbody>
</table>
<div class="col-md-12 text-center">
	<button class="btn btn-danger" onclick="location.href='@Url.Action("CreateKurs", "Home")'">Dodaj kurs</button>
</div>
